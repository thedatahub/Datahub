{% set page_title = 'Clients' | trans %}

{% block content %}
    <div class="panel panel-primary">
        <div class="panel-heading">
            <div class="btn-group pull-right">
                <a href="{{ path('datahub_oauth_clients_add') }}" class="btn btn-xs btn-default">
                    <i class="fa fa-plus"></i> {{ 'New client' | trans }}</a>
                </a>
            </div>

            <h3 class="panel-title">
                <i class="fa fa-cloud-upload"></i>
                {{ 'OAuth Clients' | trans }}
            </h3>
        </div>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>{{ 'Application name' | trans }}</th>
                    <th>{{ 'Allowed grant types' | trans }}</th>
                    <th>{{ 'Actions' | trans }}</th>
                </tr>
            </thead>
            <tbody>
                {% for client in clients %}
                    <tr>
                        <td>{{ client.getApplicationName() }}</td>
                        <td>{{ client.getAllowedGrantTypes() | join(', ') | raw }}</td>
                        <td>
                            <div class="btn-group">
                            </div>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="5">
                            {{ 'No results found.' | trans }}
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
