{% extends 'DataHubUserBundle::layout.html.twig' %}

{% set page_title = 'layout.register' | trans({}, 'DataHubUserBundle') %}

{% block content %}
<p>{{ 'registration.confirmed' | trans({ '%username%': '<strong>' ~ user.username ~ '</strong>' }, 'FOSUserBundle') | raw }}</p>

<div class="btn-group">
    <a href="{{ path('datahub_shared_default_index') }}" class="btn btn-default">
        {{ 'Continue' | trans }} <i class="fa fa-chevron-circle-right"></i>
    </a>
</div>
{% endblock %}
