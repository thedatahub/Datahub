{% extends 'VKCDataHubUserBundle::layout.html.twig' %}

{% set page_title = 'layout.register' | trans({}, 'FOSUserBundle') %}

{% block content %}
<p>{{ 'registration.confirmed' | trans({ '%username%': '<strong>' ~ user.username ~ '</strong>' }, 'FOSUserBundle') | raw }}</p>

<div class="btn-group">
    <a href="{{ path('vkc_datahub_shared_default_index') }}" class="btn btn-default">
        {{ 'Continue' | trans }} <i class="fa fa-chevron-circle-right"></i>
    </a>
</div>
{% endblock %}
