<div class="motd">
    <p>This page contains auto-generated documentation and a sandbox for the <strong>REST API</strong>.</p>
    <p>You can add <code>?_doc=1</code> to any API call URL to retrieve relevant documentation for said call.</p>
    <p>Please keep in mind that the sandbox will not work without a valid token, since the API is protected by OAuth2.</p>
    <p>For OAuth2, the authorize URL is <code>/oauth/v2/auth</code> and the token URL is <code>/oauth/v2/token</code>.</p>
</div>

<style type="text/css">
    #header {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
    }

    body {
        padding-top: 100px;
    }
</style>
